# 2026-03-08

## Daily Summary
- 给 `clawd-ai-kp` 补了一份可直接甩给外部 Codex 的交接文档：`clawd-ai-kp/docs/codex-handoff-2026-03-08.md`，并记录了当前真实链路、smoke test 状态、缺口与优先改造方向。
- 把 QQ/OneBot 主动提醒问题查实了：旧方案 `main + systemEvent + next-heartbeat` 只是“入队等 heartbeat”，不是即时送达；后续规则统一为 `cron + sessionTarget=isolated + agentTurn + deliver:true`，并且默认跟随当前聊天目标。2 分钟提醒和 5 分钟天气测试都已实收通过。
- 后台启用中的 isolated cron 子任务已统一切到 `gpt-5.4 + high`，避免继续出现 `5.3-codex low` 的历史配置干扰。
- 复核了 `ai-kp` 仓库外部改动：当前新增的核心内容是 `core/data/scenes/imported-scene.scene.json` 这类导入骨架，说明“导入 -> scene json”链路已通；项目已可测试/联调，但还没到正式稳定上线阶段。

## Important Context
- QQ 主动提醒的通用原则已纠正：默认跟随当前聊天（私聊发私聊、群聊发群），只有用户明确说“发我 QQ 私聊”时才固定 `user:281894872`。
- 现有每日天气提醒已迁到新链路，后续同类提醒默认按这套 SOP 执行。
- `ai-kp` 当前更像“联调收口期”而不是“继续堆功能期”：真实消息接线、群聊 smoke test、回环/多人并发处理，优先级高于继续扩面。
- 新导入场景目前仍是骨架文件，空缺 `areas / truthLayers / endingHooks / clues / events / nextOptions / npcRefs / starterPrompts` 等关键内容，暂时不能直接拿来跑团。

## Open Todos
- [ ] 把 `adapter/onebot/single-session` 真挂入实际消息插件链路（不只模块可测）。
- [ ] 在测试群完成 `ai-kp` 真群 smoke test，重点看回环、重复消费、多人同时发言、异常输入与消息长度。
- [ ] 把 `core/data/scenes/imported-scene.scene.json` 从导入骨架补成可玩场景，并接进 campaign / story pack / runtime。
- [ ] 拆分旧教堂文档为：玩家可见版 / KP版 / 系统版。
- [ ] 接出旧教堂后一幕（钟楼/地下/失踪案）并形成可运行钩子。
- [ ] 补多人团边缘流程：晚到、掉线重回、spotlight 权限与冲突处理。
- [ ] 补更完整状态呈现：SAN/伤势/临时异常/物品变动等。
- [ ] 持续排查 `memory/chatlog` 的 `groupName: unknown` 来源与修复方案。

## Follow-ups
- 明日优先把“正式消息接线 + 测试群 smoke test + imported scene 补全”连成一条收口线，先把可测性做扎实。
- 若后续还要看模型配置是否干净，可再决定要不要清理已停用 / 过期 cron 里残留的 `thinking: low` 历史记录。
