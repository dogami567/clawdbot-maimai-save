# 2026-03-06

## Daily Summary
- 完成 `gpt-5.4` 上下文上限排查：确认磁盘配置已是 `1,000,000`，重启网关后运行态从 `400k` 生效到 `1.0m`。
- `ai-kp` 项目方向彻底对齐：`KP` 指 TRPG/CoC 主持人，不是普通陪聊；确定“核心引擎先做，后包插件接入”。
- 规则基线确定为 `Call of Cthulhu 7th Edition`，并固定官方规则入口用于后续补充查询。
- 已围绕 CoC 流程与裁定策略沉淀首批文档（玩家全流程、最小规则、决策摘要、规则参考），并同步推进到 `clawd-ai-kp` 仓库。
- Moltbook 试水帖已发布（general 分区），当前仅见低质量/疑似 spam 回复，尚无有效技术讨论反馈。
- 定时天气播报需求更新：用户要求“提醒文本 + 实时天气结果”都要发，且发送到当前 QQ 私聊。

## Important Context
- 用户明确偏好：讨论与实现都要“先搜索，再沟通拍板”，不接受闷头写。
- `ai-kp` 玩法核心被反复确认：硬规则明确、软裁定开放、护栏控平衡；不做“前方区域以后再来吧”式硬空气墙。
- 玩家体验层关键点：车卡（含时代约束与物品灵活配额）→ 探索/NPC 对话/计时推进 → 战斗与动态裁定 → 结团存档。
- Codex 链路验证到位：任务提交与回调可用，但“产物全文自动回传到群”能力仍不稳定，当前常见是只有摘要/路径。

## Open Todos
- [ ] 修复 Codex bridge 的 `stream_read_error` 断流问题，优先保证完成态稳定回传。
- [ ] 完成按 `jobId` 的回调会话隔离（同 job 复用、不同 job 新建），避免主会话被回执文本干扰。
- [ ] 优化 Codex 产物正文回传链路：减少仅摘要回调场景，提升“直接发原文”成功率。
- [ ] 继续推进 `ai-kp`：把现有文档决策稳定映射到可运行 API/状态机层并做可复现实测。
- [ ] 排查 `memory/chatlog` 中 `groupName: unknown` 的元数据来源并改进归档质量。

## Follow-ups
- 明日优先：先验一次 Codex 回调稳定性（含正文回传），再继续 `ai-kp` 最小可玩流程的代码化收敛。
- 天气播报项需复核：确认新规则（提醒+天气结果同发到 QQ 私聊）在下一次定时执行中实际生效。
