# 2026-03-05

## Daily Summary
- 重点围绕“Clawdbot（容器）↔ 宿主机 Codex”链路做联调与体验修复，确认 webhook 主路径可用并完成多轮实测。
- 明确并固化多条长期规则：`Codex` 默认指宿主机、宿主机本地回执默认可信、Codex 回报需转为中文口语摘要+点评、不直出原始日志。
- 记录了影响体验的核心问题：`stream_read_error` 与流式输出中断，导致任务看似“断开”；方向上已转向按 `jobId` 会话隔离与状态兜底读取。
- 归档规则继续收口：Codex 任务日志仅保留“用户消息 + 最终总结”，不保留工具调用与思考过程。
- 灾备能力落地：确认并启用备份仓库与自动打包脚本，确保关键记忆/技能/脚本可恢复。

## Durable Updates Captured
- 术语映射：用户说“龙虾论坛”默认指 Moltbook。
- 口吻约束：避免 GPT 口癖与模板化命令句，保持自然、简短、同伴感表达。
- 回报格式：默认口语说明“做了什么/结果如何/下一步”，不主动暴露 `jobId` 等机器字段。

## Open Todos
- [ ] 跟进外部 Codex 内核 patch：为内置 `web_search` 增加 Exa provider（schema/types/cache/failover/test 全量核对）。
- [ ] 在同一执行环境做 Exa 连通性回归（统一入口、统一参数），验证多 key 自动切换稳定性。
- [ ] 排查并改善 chatlog 中 `groupName: unknown` 的元数据来源，降低未知群名占比。
- [ ] 复检 OneBot 图像发送链路失败重试策略，降低 SIGKILL 导致的体验抖动。
- [ ] 修复 Codex bridge 的 `stream_read_error` 断流问题（优先回调稳定性 + 状态兜底）。
- [ ] 落地按 `jobId` 的回调会话隔离（同 job 复用、不同 job 新建）。

## Follow-ups
- 明日优先：先验证 Codex bridge 回调稳定与日志完整性，再推进 Exa provider patch 回归。