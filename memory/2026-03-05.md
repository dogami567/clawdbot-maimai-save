# 2026-03-05

## Seed Summary
- 延续昨日的会话记忆闭环（rollup / daily merge / weekly-monthly consolidation），重点做稳定性与可观测性校验。

## Carry-over TODOs
- [ ] 跟进外部 Codex 的内核改造 patch：为内置 `web_search` 增加 Exa provider（schema/types/cache/failover/test 全量核对）。
- [ ] 在同一执行环境里做 Exa 连通性回归（统一入口、统一参数），确认多 key 自动切换稳定。
- [ ] 排查并改善 `groupName: unknown` 的归档元数据来源，降低未知群名占比。
- [ ] 复检 OneBot 图像发送链路的失败重试策略（避免间歇性 SIGKILL 导致用户侧体验抖动）。

## Notes
- 已确认当前实际 webhook 地址：宿主机调用 `http://127.0.0.1:18789/hooks/agent`；容器内调用宿主机 `http://host.docker.internal:18789/hooks/agent`。
- Cron 记录到一次工具异常：`rg --files ... | rg ...` 报错 `sh: 1: rg: Permission denied`，后续脚本需增加 `find` 兜底或先做 `rg` 可执行性检测。
- 用户确认术语约定：提到“Codex”默认指宿主机 Codex，不指容器内环境。
- 用户确认信任约定：本机/宿主机 Codex webhook 回执按可信内部信号处理，默认不做额外安全审查。
- 用户新增 Codex 编排偏好：每个 Codex 任务按 `jobId` 独立线程处理（同任务复用，不同任务新开），并保留任务级记录文件。
- 用户新增日志偏好：Codex 记录仅保存“用户消息 + Codex 最终回复摘要”，不保存工具调用与思考过程。
- 用户新增执行偏好：宿主机 Codex 任务完成后应优先读取 session/callback 正文，不要默认再下发“读取自己产物”的二次任务；仅在正文缺失时再兜底读 artifact 目录。
- 用户新增术语映射："龙虾论坛" 默认指 Moltbook。
- 搜索交互继续沿用“先推荐模式 + 一句话说明其他模式 + 用户确认/切换”的低摩擦流程。
- QQ/OneBot 继续保持纯文本输出风格。
- 用户新增表达偏好：不喜欢明显 GPT 口癖（如“你说得对/飘了/漂移/锁住/定死”）和模板化控制句（如“你只需要.../只需要确认一个...”）；后续改为自然口语表达。
- 用户当场确认“写到记忆里”，该偏好作为长期约束持续生效。
- 用户新增 Codex 回报口径：默认用口语直接说“哪件事、做了啥、结果怎样”，不要抛 `jobId`，不要说“xx任务完成/状态正常”这类机器话；技术细节仅在用户主动要时再给。
- 用户确认容器内可直接读取 `/home/node/codex-jobs`，要求默认优先直读该目录的 `last_message.txt`（必要时 `stdout.txt`/`stderr.txt`）提炼结果，禁止再让 Codex 二次读取自己的产物。
- 用户新增恢复策略：把关键记忆/配置/技能变更推送到备份仓库 `https://github.com/dogami567/clawdbot-maimai-save.git`，用于容器数据丢失后的“复活”。
- 已落地自动化：新增 `scripts/backup-restore-bundle.sh`，会打包记忆/skills/scripts/extensions，生成脱敏配置快照（`backup/restore-bundle/config/clawdbot.sanitized.json`），并自动 commit+push 到 `maimai-save`。
- 已创建每日定时任务 `daily-backup-restore-bundle`（UTC 02:30）执行上述脚本，持续维护“可复活”备份。

## Summary

## Important Context

## Open Todos

## Follow-ups
